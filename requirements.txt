# ============================================================
# Environment dependencies
# Covers: dust3r_to_3dgs.py + simple_trainer_prune_v2.py
# ============================================================
#
# [Installation Steps]
# 1. Install PyTorch with CUDA (choose based on your CUDA version):
#    CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#    CUDA 12.4: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#    CUDA 12.5+ / 13.x: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
#    Note: cu128 supports sm_120 (RTX 50 series) and is compatible with CUDA 12.5+ and 13.x
#
# 2. Install other dependencies:
#    pip install -r requirements.txt
#
# 3. Install dust3r (no pip package, build from source):
#    git clone https://github.com/naver/dust3r
#    cd dust3r/croco/models/curope && python setup.py build_ext --inplace
#    cd ../../..
#
# 4. Install pycolmap specific version:
#    pip install git+https://github.com/rmbrualla/pycolmap@cc7ea4b7301720ac29287dbe450952511b32125e --no-build-isolation
#
# 5. (Optional) Install fused-ssim for faster SSIM computation:
#    pip install git+https://github.com/rahul-goel/fused-ssim --no-build-isolation
#    Note: If not installed, torchmetrics SSIM will be used automatically
# ============================================================

# ---------- PyTorch not listed here, install CUDA version first ----------

# ---------- General Scientific Computing ----------
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
Pillow>=9.0
tqdm>=4.65
PyYAML>=6.0
matplotlib>=3.7
typing_extensions>=4.0
open3d>=0.17

# ---------- Image/Video IO ----------
imageio>=2.28
imageio-ffmpeg>=0.4.9

# ---------- Deep Learning Tools ----------
torchmetrics>=1.0
lpips>=0.1.4

# ---------- Training Framework/Visualization ----------
tyro>=0.6.0
viser>=0.1.0
tensorboard>=2.13

# ---------- 3DGS Related ----------
plyfile>=0.9
tensorly>=0.8.0
# gsplat: Install from source with auto-detect GPU architecture
# pip install git+https://github.com/nerfstudio-project/gsplat.git --no-build-isolation

# ---------- nerfview (gsplat viewer dependency) ----------
nerfview
splines

# ---------- DUSt3R Dependencies ----------
einops>=0.6.1
huggingface_hub>=0.19.0
safetensors>=0.4.0
roma>=1.3.0
opencv-python>=4.8
trimesh>=3.21

# ---------- Optional: PNG Compression ----------
# torchpq
# plas